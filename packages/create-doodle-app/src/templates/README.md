# Doodle app

This project was created with Doodle DS. Start with `npm run dev`, then edit `src/App.tsx`.

See `DESIGN_SYSTEM.md` for the visual contract. For an existing Vite React app, install the Doodle packages manually and copy the Vite plugin and CSS setup from this project.
