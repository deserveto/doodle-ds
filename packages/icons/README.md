# @doodle-ds/icons

Scribbles is the hand-drawn icon set for Doodle DS.

```tsx
import { SearchIcon } from "@doodle-ds/icons";

<SearchIcon className="h-5 w-5" />;
```

Source SVGs live in `assets/svg/`. React components are generated during build.
