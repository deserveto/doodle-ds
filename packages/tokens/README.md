# @doodle-ds/tokens

Doodle DS design tokens in DTCG JSON format, compiled to CSS custom properties.

```css
@import "@doodle-ds/tokens/css/tokens.css";
```

The `.dark` class switches semantic values. Override `--ds-*` variables to brand the system.
